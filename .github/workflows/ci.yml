name: ci

on:
  push:
  pull_request:

jobs:
  format:
    name: format
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: sbt
      - uses: sbt/setup-sbt@v1
      - name: Check Scalafmt
        run: sbt -batch fmtCheck

  test:
    name: ${{ matrix.platform }}
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          - platform: JVM
            tasks: coreJVM/test designsJVM/test apiJVM/test lawsJVM/test benchmarks/test
          - platform: Scala.js
            tasks: coreJS/test designsJS/test apiJS/test lawsJS/test
          - platform: Scala Native
            tasks: coreNative/test designsNative/test apiNative/test lawsNative/test

    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: sbt
      - uses: sbt/setup-sbt@v1
      - name: Install Scala Native toolchain
        if: matrix.platform == 'Scala Native'
        run: sudo apt-get update && sudo apt-get install -y clang lld
      - name: Compile and test
        run: sbt -batch ${{ matrix.tasks }}

  compatibility:
    name: compatibility baseline
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"
          cache: sbt
      - uses: sbt/setup-sbt@v1
      - name: Check MiMa and TASTy-MiMa
        run: sbt -batch compatibilityAll
